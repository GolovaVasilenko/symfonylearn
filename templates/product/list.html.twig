{% extends 'base.html.twig' %}
{% block title %}

{% endblock %}

{% block body %}
    {% for product in products %}
        <h3>{{ product.name }}</h3>
        <p>{{ product.price }}</p>
        <p>{{ product.description }}</p>
    {% endfor %}

{% endblock %}



