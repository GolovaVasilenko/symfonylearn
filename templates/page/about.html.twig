{% extends "base.html.twig" %}
{% block title %}{{ title }}{% endblock %}
{% block body %}
<h1>{{ h1 }}</h1>
    <div class="content">
        {{ body }}
        <p><a href="{{ link }}">Notebook</a></p>
        <p><a href="{{ path('category_show', {'slug': 'tvs', 'page': 5}) }}">TVs</a></p>
        <p><a href="{{ url('category_show', {'slug': 'media', 'page': 10}) }}">Nedia</a></p>

        <p>Last Visited Category: {{ last_visit }}</p>
    </div>
{% endblock %}