{% extends 'base.html.twig' %}

{% block title %}
{{ title }}
{% endblock %}

{% block body %}
{% for category in categories %}
<h3>{{ category.name }}</h3>
    <p>{{ category.description }}</p>
    <a href="{{ path("category_show_by_id", {'id': category.id}) }}">Open Category</a>
    <hr>
{% endfor %}
{% endblock %}

